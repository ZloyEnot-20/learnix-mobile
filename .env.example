# Backend REST API — must include the /api suffix.
# For local dev on a physical device use your PC's LAN IP instead of localhost.
EXPO_PUBLIC_API_URL=https://api.learnix.space/api

# Optional: set after `eas init` (EAS project ID from expo.dev)
# EAS_PROJECT_ID=
# EXPO_OWNER=
